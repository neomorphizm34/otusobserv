- name: playPrometheusSrv
  hosts: prometheus
  tasks:
    - name: Install Git
      become: true
      yum:
        name: git
        state: present

    - name: Git checkout
      ansible.builtin.git:
        repo: 'https://github.com/neomorphizm34/otusobserv.git'
        dest: /tmp/checkout